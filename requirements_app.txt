# SteelScan AI — Requirements (CPU-only, no GPU needed)
# ========================================================
# Install:  pip install -r requirements.txt
# Python:   3.9 – 3.11  |  Works on Windows / macOS / Linux

# Web App
streamlit>=1.32.0

# AI (CPU build auto-selected when no GPU is found)
ultralytics>=8.1.0
torch>=2.1.0
torchvision>=0.16.0

# Image processing
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.24.0

# Utilities
pyyaml>=6.0
